# domain-checker

Project containing a prototype domain checker, which goes to a web domain and extracts different attributes relating to the site or page.


The input to the tool is a csv containing a list of domains as text or strings. The domain nma emust be in the format: 'domain.com'. 

The tools then outputs a CSV containing the domain information.
